Frontend:

TypeScript + React 19 — main UI language
TanStack Start v1 (Vite 7) — full-stack React framework with SSR + file-based routing
TanStack Router + TanStack Query — routing & data fetching
Tailwind CSS v4 — styling (via src/styles.css)
shadcn/ui + Radix UI — component library
Lucide React — icons
Zod — form/data validation
Sonner — toast notifications
Backend (Lovable Cloud):

Supabase (Postgres database, Auth, Storage)
SQL — database migrations (supabase/migrations/)
TanStack Server Functions (createServerFn) — server-side RPC logic in TypeScript
Deploy target: Cloudflare Workers (edge runtime)
Build/tooling:

Vite 7 — build tool
Bun — package manager / runtime
ESLint + Prettier — linting/formatting
Languages summary:

TypeScript / TSX (primary)
SQL (migrations)
CSS (Tailwind v4)
HTML (root shell)
